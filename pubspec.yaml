name: kohi
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  json_annotation: ^4.8.1
  provider:
  lottie: ^2.7.0
  flutter_view_widgets: ^2.2.5-dev.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  json_serializable: ^6.7.1
  build_runner: ^2.4.6

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/images/
    - assets/images/latte.png
    - assets/json/
    - assets/json/coffee.json
